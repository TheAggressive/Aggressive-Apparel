export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
# Fast, per-commit autofix only. The heavy suite (tests + full lint +
# PHPStan) runs on pre-push and in CI — see .husky/pre-push.
pnpm run format:fix && pnpm run lint:js:fix
