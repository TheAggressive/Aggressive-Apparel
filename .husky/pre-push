export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
# Full required-CI rehearsal. It uses the exact CI lane commands and an
# isolated wp-env, so local development uploads and databases are untouched.
pnpm run ci:verify
