export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
# Heavy gate, kept off the commit path so commits stay fast. Mirrors the
# CI gate: i18n (POT drift + PO validity), Composer lock installability,
# full test suite (JS + PHP), all linters, typecheck, and PHPStan. Cheap,
# environment-independent checks run before the test suite. Requires wp-env
# running for the PHP tests (pnpm env:start).
pnpm run qa
