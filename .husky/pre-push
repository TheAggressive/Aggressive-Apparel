export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
# Fast gate (~3 min, measured): lint, types, JS tests, drift contract, build, PHP.
# Browser E2E and packaging are CI-only — see bin/ci/verify-fast.sh for why.
# Before cutting a release, run the full rehearsal yourself: pnpm qa
pnpm run qa:fast
