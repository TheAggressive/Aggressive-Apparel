/**
 * Nav Link Block Save Component
 *
 * Returns null — rendered server-side via render.php.
 *
 * @package Aggressive_Apparel
 */

/**
 * Dynamic block — no static save output.
 *
 * @return null Always returns null for server-side rendering.
 */
export default function Save() {
  return null;
}
