/**
 * Nav Mega Menu Block View Script
 *
 * Desktop-only mega menu. Registers the navigation store so the block's
 * Interactivity API directives resolve correctly.
 *
 * @package Aggressive_Apparel
 */

import '../navigation/store';
