/**
 * Nav Submenu Block View Script
 *
 * A nav-submenu can live in the desktop navigation block (navigation store) or
 * in the mobile navigation-panel block (navigation-panel store). We register
 * both stores so the submenu's directives resolve in either context.
 *
 * @package Aggressive_Apparel
 */

import '../navigation/store';
import '../navigation-panel/store';
