/**
 * Navigation Mega Menu Block - Interactivity API Store
 *
 * Mega menu uses the same actions as submenu since behavior is similar.
 * The submenu view.ts already registers these actions, so this file
 * only needs to exist for the webpack entry point.
 *
 * @package Aggressive Apparel
 */

// Import submenu view to ensure actions are registered
import '../navigation-submenu/view';
